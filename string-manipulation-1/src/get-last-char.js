/* exported getLastChar */

// get last character of string and assign to variable --completed
// return variable --completed

function getLastChar(string) {
  var lastChar = string.charAt(string.length - 1);
  return lastChar;
}
