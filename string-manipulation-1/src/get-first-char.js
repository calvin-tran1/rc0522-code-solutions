/* exported getFirstChar */

// grab first character of string and assign to variable --completed
// returns variable --completed

function getFirstChar(string) {
  var firstChar = string.charAt(0);
  return firstChar;
}
