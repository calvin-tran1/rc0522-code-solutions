/* exported capitalize */

// capitilize string and assign to variable
// return variable

function capitalize(word) {
  var capital = word.charAt(0).toUpperCase() + word.slice(1).toLowerCase();
  return capital;
}

// capitalize first letter --completed
// had to slice rest of string and replace with lowercase -- completed
// assign concactenation to a variable -- completed
// return variable --completed
