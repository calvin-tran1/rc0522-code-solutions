/* exported isUpperCased */

// create if/else statement to determine whether string is uppercased --completed
// return value --completed

function isUpperCased(word) {
  if (word === word.toUpperCase()) {
    return true;
  } else {
    return false;
  }
}
