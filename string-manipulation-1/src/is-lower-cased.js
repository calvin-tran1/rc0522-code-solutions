/* exported isLowerCased */

// create if/else statement to determine whether string begins with a lowercase --completed
// return value --completed

function isLowerCased(word) {
  if (word === word.toLowerCase()) {
    return true;
  } else {
    return false;
  }
}
