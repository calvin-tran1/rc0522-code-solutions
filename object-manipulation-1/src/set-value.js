/* exported setValue */

// use bracket notation to push key into object --completed
// assign the value to the pushed key --completed
// return that expression

function setValue(object, key, value) {
  object[key] = value;
}

// function does not contain return statement
