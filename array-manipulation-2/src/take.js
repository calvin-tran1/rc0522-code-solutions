/* exported take */

// use slice to take listed number of elements from array --completed

function take(array, count) {
  return array.slice(0, count);
}

// take elements from array starting from the given argument
// return new value
