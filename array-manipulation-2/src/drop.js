/* exported drop */

// slice array starting from count --completed

function drop(array, count) {
  return array.slice(count);
}

// drop elements from array starting at the given argument
// return that new value
